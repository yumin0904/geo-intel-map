# Phase 0 기본 패키지 (2026-05-19 기준 최신 안정 버전)

# 웹 프레임워크
fastapi==0.136.1

# ASGI 서버 (standard = websockets, httptools 포함)
uvicorn[standard]==0.47.0

# 비동기 HTTP 클라이언트 (외부 API 호출용)
httpx==0.28.1

# 데이터 검증 (v2: Event 모델 등 Pydantic BaseModel에 사용)
pydantic==2.13.4

# .env 파일에서 API 키 로드
python-dotenv==1.2.2

# 비동기 SQLite (Phase 1 DB)
aiosqlite==0.22.1

# 시장 지표 — Cascade 룰의 response(유가 CL=F 등) 검증용 (무료, Phase 2)
yfinance==1.3.0

# TheoryLibrary — 마크다운 YAML front matter 파싱 (Phase 3)
python-frontmatter==1.0.1

# 백그라운드 스케줄러 — 아카이브 TTL, 데이터 갱신 크론 잡 (CLAUDE.md §4)
apscheduler>=3.10,<4.0

# Granger 인과분석 — Cascade 룰 통계 검증 (Phase 3, cascade/correlation.py)
statsmodels>=0.14,<1.0

# IA-Engine-B1: SIPRI·Kiel Excel 파싱 (load_external_data.py)
openpyxl>=3.1,<4.0
